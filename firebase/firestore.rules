rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Configurações do jogador - somente usuários autenticados podem ler/escrever
    match /configJogador/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}